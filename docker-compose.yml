services:
  # MySQL Database
  mysql:
    build:
      context: .
      dockerfile: Dockerfile.mysql
    image: cbl-so-mysql
    container_name: cbl-so-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: cbl_so_root_password
      MYSQL_DATABASE: cbl_so
      MYSQL_USER: cbl_so_user
      MYSQL_PASSWORD: cbl_so_password
    ports:
      - "3307:3306"
    volumes:
      - cbl-so-mysql-data:/var/lib/mysql
      - ./database.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - cbl-so-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # Backend API
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: cbl-so-backend
    container_name: cbl-so-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: cbl_so_user
      DB_PASSWORD: cbl_so_password
      DB_NAME: cbl_so
    ports:
      - "3002:3001"
    volumes:
      - ./uploads:/app/uploads
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - cbl-so-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3001/health', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Frontend React App
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    image: cbl-so-frontend
    container_name: cbl-so-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - cbl-so-network

volumes:
  cbl-so-mysql-data:
    driver: local

networks:
  cbl-so-network:
    driver: bridge
